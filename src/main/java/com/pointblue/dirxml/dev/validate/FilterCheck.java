package com.pointblue.dirxml.dev.validate;

import com.pointblue.dirxml.dev.model.DriverSet;

/** Driver filter and schema map structure and consistency. See docs/validation.md. (Stub — implementation pending.) */
public final class FilterCheck implements Check {

    @Override
    public String name() {
        return "filter";
    }

    @Override
    public void run(DriverSet ds, Report r) {
    }
}
