{
  "name": "dirxmldev-visual",
  "description": "Fishbone tool names implemented by the DirXMLDev MCP server (mcp/dirxmldev-mcp, docs/mcp.md). fishbone.get and fishbone.reveal shell out to bin/idm query; fishbone.refresh writes only the .dirxmldev/fishbone.refresh sentinel. The VS Code commands remain the editor-side hooks.",
  "mcpServer": {
    "package": "mcp/dirxmldev-mcp",
    "docs": "docs/mcp.md",
    "command": "node",
    "args": ["mcp/dirxmldev-mcp/src/index.js"]
  },
  "tools": [
    {
      "name": "fishbone.get",
      "description": "Return the Designer-style policy-flow fishbone JSON for one driver from an IDM-as-code tree (driverset.xml).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "tree": {
            "type": "string",
            "description": "Absolute path of the as-code tree root."
          },
          "driver": {
            "type": "string",
            "description": "Driver name; omitted = first driver in driverset.xml."
          }
        },
        "required": [
          "tree"
        ]
      },
      "run": "mcp/dirxmldev-mcp: bin/idm query <tree> drivers --json when driver is omitted, then bin/idm query <tree> fishbone <driver> --json"
    },
    {
      "name": "fishbone.refresh",
      "description": "Reload an open fishbone webview from disk after agent edits. Does not write policies.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "tree": {
            "type": "string",
            "description": "As-code tree root (sentinel is written here)."
          },
          "driver": {
            "type": "string"
          }
        },
        "required": [
          "tree"
        ]
      },
      "run": "touch <tree>/.dirxmldev/fishbone.refresh   # or VS Code command dirxmldev.fishbone.refresh"
    },
    {
      "name": "fishbone.reveal",
      "description": "Resolve a fishbone node (bone key, policy ref, or node id) to a content file path for the editor to open.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "tree": {
            "type": "string"
          },
          "driver": {
            "type": "string"
          },
          "ref": {
            "type": "string",
            "description": "Artifact path (drivers/D/subscriber/\u2026), bone key (subscriber-event), or node id."
          }
        },
        "required": [
          "tree",
          "driver",
          "ref"
        ]
      },
      "run": "mcp/dirxmldev-mcp: bin/idm query <tree> fishbone <driver> --json, then resolve ref to a content file under the tree",
      "vscodeCommand": "dirxmldev.fishbone.reveal"
    }
  ]
}
